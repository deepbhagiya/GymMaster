<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gymfitnessapp"
        tools:targetApi="31">

        <!-- Step 1: Environment Selection (Gym vs Home) - The Start Screen -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Step 2: Category Selection (Chest, Legs, etc.) -->
        <activity android:name=".CategoryActivity" />

        <!-- Step 3: Filtered Exercise List -->
        <activity android:name=".ExerciseListActivity" />

        <!-- Step 4: Video and Description -->
        <activity android:name=".DetailsActivity" />

        <!-- New Feature: Suggestions based on BMI -->
        <activity android:name=".SuggestActivity" />

    </application>

</manifest>
